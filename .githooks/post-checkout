#!/bin/bash
# Post-checkout hook to refresh .title file whenever branch changes

# Only run if switching branches (not file checkout)
if [ "$3" == "1" ]; then
    echo "Updating .title file after branch switch..."
    python scripts/update_title.py
fi
